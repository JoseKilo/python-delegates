# Python Delegates

Delegates implementation for Python

This packages provides a simple way to write delegates for Python. Slightly
inspired by C# delegates and similar to function pointers in C/C++.

A delegate declaration defines a function signature.
A delegate instantion contains a function implementation.
